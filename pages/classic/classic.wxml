<view class="classic-container">
  <like
    wx:if="{{classicData.content}}"
    like="{{classicData.like_status?true:false}}"
    count="{{classicData.fav_nums}}"
  ></like>
  <text
    >{{ classicData.like_status
    }}{{ classicData.like_status ? true : false }}</text
  >
</view>
